# Tasks

## Current
- [x] Level 20 Demon Slayer ID-card art — first card-type ID and the platform that makes future cards drop-in. User: "i have added a image for the first test of the id it is for the one you unlock at level 20 ... there is a blacnk spot for the plaeyr id if should fit in that ther is small detail like a little golden fram around wher the player id phot will go do not cover that fram up witht e phot it shoud fit inside ... a little bit of extra spavce roun the id itselfe you will have to be detialed and trim that out without removing the fram ... these rules wil applyu to all the ids i will name them a leve they shoudl not have to option to see or use htem unless they re at or higher than the leve of preseiege". `ID_BACKGROUNDS` extended with `type:"card"` shape carrying `path`, `prestigeReq` (int), `trim:{x,y,w,h}` (source pixels — content-rect crop that drops the outer black padding while preserving the gold frame), `photoSlot:{x,y,w,h}` (source pixels relative to trim — where the player photo plate sits inside the trimmed art) and `photoInsetPx` (post-scale margin so the photo never covers the gold frame). New entry `demon_slayer` points at `sprites/id_cards/level_20_demon_slayer.png` with `prestigeReq:20`, `trim:{x:18,y:7,w:1741,h:872}`, `photoSlot:{x:121,y:171,w:386,h:541}`, `photoInsetPx:4` — measured by Python/PIL using gold-frame and dark-photo-plate detection on the 1774×887 source. Three new helpers next to `_drawIdPhotoBackground`: `_isIdBgUnlocked(bg)` (true when `_prestigeCount >= bg.prestigeReq||0`); `_getIdCardPhotoRect(bg, x, y, w, h)` (scales `photoSlot` from `trim` space into the card's draw rect, applying `photoInsetPx` on each edge); `_drawIdCardArt(c, bg, x, y, w, h)` (drawImage with source = trim rect → returns the computed photo rect, or null if the image hasn't loaded). `getActiveIdBackground` now silently falls back to the default if the saved bg is locked (prevents stuck state if prestige somehow regresses). `_drawIdPhotoBackground` now samples the photo region of the card art when the active bg is `type:"card"`, so the 96×96 photo box in the procedural fallback (and small swatch boxes) shows the card's plate texture instead of a stale gradient. Lobby ID card render at `index.html:22638` refactored to branch: when active bg is type:"card" and the image is loaded, drop shadow → drawImage(trimmed) → composite player photo at the scaled photoSlot rect → skip the beige fill / red header / amber frame / thumbtack (the card art owns its own ornamentation); else procedural beige+red layout untouched. Agent text colors swap based on layout (gold/cream over dark card vs brown over beige) so name/rank/investigations/XP-bar/next-unlock-teaser stay legible on either backdrop. ID tab BACKGROUND list at `index.html:22440` filters via `_isIdBgUnlocked` so locked cards are hidden entirely (per "shoud not have to option to see or use") — no greyed-out rows, the row simply doesn't exist until the player has the prestige. Background swatch widened from 36→56 px and now draws the full trimmed card image at small scale for type:"card" entries (matches what the player will equip). Right preview pane at `index.html:22517` branches on the active bg type: card-type renders the whole card scaled to fit the pane (computed from `trim` aspect ratio) with the player photo composited in at the right slot; gradient/image bgs keep the 96×96 photo-box preview. All four smoke suites pass (`node tests/run-smoke.js`).
- [x] Character ID-card portraits. User: "i am adding 6 picture one if for each charecters profile they should be named for each personf put them where they need to go in the folder to be orginized and then that image shoud be for the id photo the one on the pe game lobby on the player id card it shoud change automaticly based on the charecte you are using". Wired the lobby ID card photo box ([index.html:22322](index.html#L22322)) to read a per-character `portrait.png` from each character's existing folder under `sprites/characters/<name>/portrait.png` (lowercase: john, chad, tiffiny, loren, candy, samuri). Preload at startup mirrors the existing PIXELLAB_CHARS walk-frame preload pattern: `_plPortraitImgs[ch.id]` = preloaded `Image()` per char. Draw path checks `_portrait.complete && _portrait.naturalWidth >= 32` — if a real portrait is present it scales to the 96×96 photo box; otherwise it falls back to the existing PixelLab south-facing sprite. The `>= 32` size guard treats 1×1 stub PNGs as missing so the fallback kicks in until the user drops their real art. Six 1×1 transparent PNG stubs created at the target paths so the network probes succeed (no console 404s polluting the smoke tests). When the user replaces any stub with their real art, the portrait shows up automatically on the ID card based on `_selectedChar`. All four smoke suites pass.
- [x] Per-tier mechanics wiring pass — Phasmo-canonical T1/T2/T3 functional deltas. After the previous session shipped T2/T3 art for all 21 items and calibrated the EQUIPMENT_DATA stat blocks, this session closed the remaining functional gaps so the tier numbers actually drive gameplay. **Photo Camera** — added `_photoShotCooldownT` shutter cooldown (per-frame tick + gate at the R-press shutter) reading per-tier `flashCooldownSec` (T1=0.6 s, T2=0.5 s, T3=0.4 s), and the ghost-photo star rating now multiplies by per-tier `photoQuality` (T1=1.0, T2=1.25, T3=1.5) clamped to [1, 3] — same shot from a T3 camera promotes one star band closer to 3 stars. **Crucifix** — new `hasActiveCursedHuntBlocker()` helper iterates `worldItems` for placed unspent Crucifixes and checks each instance's `_tier` block for `blocksCursedHunts:true` (T3 only); both cursed-hunt origin paths now consult it — Ouija "no" / dangerous-question hunt (28 % roll) and Summoning Circle finale hunt are both suppressed when a T3 Crucifix is placed and unspent. **Sound Sensor** — per-frame trigger now reads the equipped tier's `showsLocation`; T3 reports the *ghost's* current room rather than the sensor's room, so the van display pinpoints the source instead of the receiver. **Sanity Meds** — added `speedBoostMult:1.35` + `speedBoostSec:6` to T3 stat block (adrenaline syringe behaviour); consume path now sets `_adrenalineTimer` + `_adrenalineMult` when those fields are present, and the player-speed calc multiplies `spd` by the active boost factor while the timer is positive. T2/T3 multi-dose was already wired. **Salt** — placement-charge initialisation now reads `pilesPerContainer` from the equipped tier (T1=3, T2=5, T3=8) instead of the hardcoded `ITEM_MAX_CHARGES["Salt"]=3`; `stepRadiusPx` per-tier was already wired. **Lighter** — added `weatherProof` field per tier (T1/T2 false, T3 true). New `_isPlayerWeatherExposed()` checks `_activeWeather` ∈ {rain, storm} AND player is outside (no room or "Yard"); `_lighterCanLightNow()` blocks ignition when the lighter isn't `weatherProof` and the player is exposed. Both strike paths gated: manual R-press (toast "Too wet to light") and auto-strike when igniting Incense. **Tier-based ghost-interaction modifier** — verified delivered by existing per-item per-tier stats (`responseChance`, `writeRollPerSec`, `orbVisibleRangePx`, `lerpRatePerSec`, `photoQuality`, `effectiveRadiusPx`, `coneDeg`, `recordRangePx`, `rangePx`) all consumed via `_itemTier1()` which now resolves the equipped tier; no new global modifier needed since the architecture already delivers higher-tier-=-better-detection per-canon. All four smoke suites pass (`node tests/run-smoke.js`).
- [x] Tier-1 item art rewrite (Phasmophobia style). User: "redo the item make it soo all the tiems look like the phasmaphhobia item for now just do all the tier 1". All 21 tier-1 items in `_drawItemArtCentered(it)` rewritten to Phasmo-canonical top-down silhouettes using a consistent style: drop-shadow ellipse + black outline (lineWidth ~1) + 2-3 tone fills (highlight / mid / shadow) + stateful detail (LED glow, lens illumination, flame, screen content). Items: Flashlight (yellow grip), UV Flashlight (purple LED face), Video Camera, D.O.T.S. Projector (3×3 green grid), Sound Recorder (mic grille), EMF Reader (5-LED bar), Spirit Box, Thermometer, Ghost Book (closed leather + open pages), Lighter (corrected to matchbook per `TIER1_ITEMS["Lighter"]` spec — was Bic), Incense, Crucifix (charges-aware palette), Sanity Meds (white-cap amber bottle + Rx label), Tripod (3-leg + hub), Salt (mound + footprint when stepped), Motion Sensor (fresnel strip), Sound Sensor (mic grille + yellow label), Firelight (pillar candle + holder), Photo Camera (DSLR + flash + lens), Head Gear (strap arc + forehead camera), Parabolic Microphone (grip + body + dish). All four smoke suites pass.
- [x] Controls debug suite + T-conflict fix. User: "run a debug on all the new controlls". Added `tests/controls-debug.test.js` (wired into `tests/run-smoke.js`) — boots the game with `?autoStart=1&skipSetup=1&debug=1` and asserts the full keybind contract: `KEYBIND_DEFAULTS` shape (T/F/E/J/G/Q/Shift/Space), `CONTROLS_SCHEMA_VERSION=3`, T toggles flashlight (round-trip), F activates held flashlight, hotbar 1/2/3 jump selectedSlot directly, Q cycles, G drops, J round-trips journalOpen, Space+L-click activates `placementPreview` + `_modPlacementActive`, and releasing Space mid-drag still commits the placement on left mouseup. New `__debug.controlsSnap()` helper exposes the relevant state in one call. **Bug caught.** Moving flashlight from F→T collided with the in-game debug HUD's controls-panel toggle (also bound to T, gated by `DEBUG_GHOST_BEHAVIOR`). With `?debug=1` the T press was hijacked by `_dbgControlsOpen` and never reached the flashlight handler. Remapped the debug-controls toggle to backtick (`` ` ``/`~`) — unused, comfortable dev key — and updated the on-canvas label "Debug Controls [`]" to match. All four smoke suites now pass.
- [x] Trackpad right-click modifier. User wants to play on a laptop trackpad without a real right mouse button: "add a special key for my game so that I can hit an unused key just fine on and then left click while holding it and it will work the same as a right click so i can play this on track pad". Added a new `rightClickMod` keybind defaulting to **Space** (an unused key in the canon Phasmo layout). Holding the modifier and pressing left mouse button now triggers the same placement-preview path as right-click; releasing left mouse with the modifier-placement active commits the placement (a left-button mouseup with `_modPlacementActive=true` re-dispatches as a synthetic button-2 mouseup, reusing the existing right-click commit handler verbatim). The modifier-aware mousedown is registered in the *capture phase* with `stopImmediatePropagation()` so other left-click handlers don't also fire on the same event. Modifier release mid-drag is fine — `_modPlacementActive` is the gate, not `keys[modifier]`. Space's default page-scroll is suppressed via `e.preventDefault()` in the keydown handler when the pressed key matches `keybinds.rightClickMod`. New row added to the journal Pause Menu controls panel — "Right-Click Mod (hold + L Click)" — fully rebindable; the renderer humanizes Space to "Space" instead of an empty `[ ]` bracket. `CONTROLS_SCHEMA_VERSION` bumped from 2 → 3 so existing saves pick up the new default. All three smoke suites still pass.
- [x] Phasmophobia control parity (keybind alignment). User asked: "look at the phasmaphobia controlles and math my game to the phasmaphobia controlled to all the keyboard inputs". Aligned Haunted's keyboard layout to current Phasmophobia (April 2026, post-Winter's-Jest v0.15.x) canon defaults: WASD move, LShift sprint, E interact/pick up, F secondary use of held item, T toggle ambient flashlight, G drop, Q cycle inventory, J journal, 1/2/3 direct hotbar select, mouse wheel cycle. **What changed.** (1) `KEYBIND_DEFAULTS` flipped `flashlight: "f" → "t"` and `use: "r" → "f"` to match Phasmo. (2) Added `CONTROLS_SCHEMA_VERSION = 2`; `loadControls` rejects saved keybinds when the persisted schema is below current, so existing players with old-default localStorage get the new bindings on next load instead of being stuck on F-flashlight / R-use forever. Sensitivity / volumes / cursor mode preserved across migration. (3) Added Phasmophobia-style direct hotbar select: pressing `1` / `2` / `3` jumps `selectedSlot` straight to that inventory index (still alongside the existing Q-cycle + mouse-wheel cycle). Slot 4 is no longer the lighter — lighter is auto-equipped via the held-item logic now that hotbar slots are direct. (4) Pause Menu controls panel relabeled: rebindable rows now read "Interact / Pick Up", "Toggle Flashlight", "Use Held Item"; fixed-key rows pruned the stale "Q Lighter / 4th Slot" / "R (board)" / "R (doll)" entries and added "1 / 2 / 3 Select Inventory Slot", "Wheel Cycle Inventory Slot", "L Click Place Pin / UI Click", "R Click Place Held Item". (5) CCTV bottom-bar prompt + camcorder REC hint + truck-monitor REC hint now read `keybinds.use.toUpperCase()` live instead of hardcoded "R", so display matches whatever the player has bound. All three smoke suites pass.
- [x] Evidence-leak audit + Mare-UV fix. User reported getting UV evidence on a Mare round. Mare's canonical evidence is [Spirit Box, Ghost Writing, Ghost Orb] — no UV — and the `GHOST_REGISTRY` data was correct, but several gameplay spawn paths were placing evidence effects in the world without checking the active ghost's evidence list. **What was fixed.** (1) UV Flashlight ItemInteract was placing a fingerprint smudge on the nearest door regardless of evidence — now gated on `currentGhost?.evidence?.includes("Ultraviolet")`. (2) Video Camera ItemInteract was pushing four ghost orbs into the fav room unconditionally — now gated on Ghost Orb evidence (or Mimic `mimicFakeOrb`). (3) Centralized helpers `_canEmitEmf5(ghost)` (checks `EMF Level 5` in evidence + not hidden + not Hantu's `no_emf5`) and `_emfLevel(requested)` (clamps level 5 → 4 when not allowed) added next to `getGhostBehavior`; seven production-path `level: 5` EMF pushes routed through `_emfLevel(5)` — music-box hunt trigger, hunt-active EMF tick, Wraith teleport, Banshee scream event, ghost-event-phase scream, body-pin tarot trigger, ouija-break cursed hunt, plus the Fiend / Reaper / Noose tarot cards. (4) Tarot "Spire" `case "activity"` was bumping `dotsDisturbTimer` regardless of D.O.T.S. evidence — now gated. (5) `_nowFreezing` evidence flag and the Photo Camera Freezing-Temperature capture both now require `currentGhost?.evidence?.includes("Freezing Temperatures")` (defense in depth — temps shouldn't drop below 1 °C on a non-freezing ghost given the recent temp rewrite, but FP drift could leak a stray reading). **Verified in audit.** All 27 ghost evidence sets in `GHOST_REGISTRY` match current Phasmo canon (April 2026, post-Winter's-Jest v0.15.x) via pcgamesn + gamerant. Journal evidence is single-sourced from `GHOST_REGISTRY` via `_journalGhostData()` so the journal display is canonical by construction. **Untouched by design.** Debug `_triggerForceItemInteract` paths (gated by `DEBUG_GHOST_BEHAVIOR`) — that helper is for runtime testing of ghost interactions and shouldn't enforce evidence rules. Jinn breaker-drain `level: 5` push left as literal 5 since Jinn always has EMF 5 evidence by identity. All three smoke suites pass.
- [x] Phasmophobia-canon temperature rewrite. Per-room temperature now follows current-Phasmo numerics from end-to-end. **Cooling** (ghost in room): −0.1 °C/s while curT > ambient, ⅓ rate (−0.0333 °C/s) once curT ≤ ambient. **Warming** (ghost not in room, curT < ambient): +0.045 °C/s when fuse ON, +0.025 °C/s when fuse OFF. **Floor**: ghost's favourite room reaches −10 °C iff Freezing Temps is in the evidence set, else 1 °C; non-favourite rooms the ghost roams floor at 1 °C. **Ambient by weather** (`WEATHER_AMBIENT_TEMP_C`): clear/fog/blood_moon = [8, 15], rain/storm = [8, 10.7], snow = [5, 7], sunrise = [10, 18]; `getAmbientTemp` returns the midpoint, `getAmbientTempRange` returns the bounds. Round init resolves `_activeWeather` from the difficulty (random pick from `["clear","fog","rain","snow","storm"]` if difficulty weather is "random") and seeds every room: 20 °C if `fuseBoxStartState==="on"`, else ambient mid + small jitter. **Thermometer** still applies the player's tier I `lerpRatePerSec` + `readingNoise` so T2/T3 thermometers feel snappier and quieter. **Thresholds tightened to canon**: breath visibility now triggers at ≤5 °C (was ≤8); both Freezing-Temps confirmations (photo capture in `ItemInteract Photo Camera` and the `_nowFreezing` evidence flag in the per-frame check) now require `< 1 °C` strictly (was `≤ 0`), so a 0.99 °C reading correctly counts as Freezing Temperatures evidence. Frost vignette retained at `≤ 0` so the visual frost-glow is still gated by hard freezing rather than the slightly looser evidence threshold. All three smoke suites pass.
- [x] Money parity with Phasmophobia (1:1 with XP). `_endRound` no longer calls a separate cash formula — it now banks `addPlayerMoney(finalXpAwarded)` straight from the XP breakdown total, mapping each XP row to a money row of equal `amount`. Old `calculateContractMoneyReward` removed as dead code. Photo-quality multiplier still applies to the XP that flows into both the XP bar and the wallet, since money == XP one-for-one.
- [x] Tier-feature wiring pass (Task 3 — autonomous gap closure). Took the shipped tier data layer and wired ten previously-stub tier deltas into live gameplay so Tier 2/3 purchases now visibly *do* something. **Crucifix:** placed crucifixes are now stamped with their `_tier` at placement time (both placement code paths) so swapping equipped tier mid-round doesn't retroactively buff/nerf placed crucifixes; both consumer sites (ItemInteract Crucifix flash + hunt-start block at index.html:8853) now iterate `worldItems` and call `getCrucifixRangePx(currentGhost, it)` per-instance, honoring T1=3 / T2=4 / T3=5 tiles + Demon ×1.5 + Banshee `crucifix_always_works → Infinity`. **EMF Reader:** held-panel readout now reads the active tier's `showsDistance` / `showsDirection` flags — T2 adds a `"X.Xm"` distance line under the level, T3 adds an 8-direction arrow (→ ↘ ↓ ↙ ← ↖ ↑ ↗) pointing to the nearest active EMF source. **UV Flashlight:** held-cone range now scales with tier `rangePx`; T3 `hasWhiteLight` adds a secondary white-light cone on top of the UV cone so the flashlight doubles as illumination. **Spirit Box:** held-listen window now reads `listenWindowSec` (T1=2.5 / T2=3.0 / T3=3.5); T2/T3 `voiceActivated` re-arms the listen automatically while the box is held + active so the player doesn't need to mash R. **Ghost Book:** T3 `hasIndicatorLed` adds a small green LED bead on the spine that pulses bright when the ghost is within `effectiveRadiusPx`, dim otherwise — gives the player a placement-quality signal at a glance. **Video Camera:** N-key NV toggle (and the CCTV monitor NV button) now gated on `hasNightVision`; T1 attempts pop a `"Night vision requires Tier 2+ camera"` toast, T2/T3 work as before. (Orb visibility range was already pulling from the tier resolver via `_itemTier1`, so T2/T3 wider cones flowed automatically.) **Tripod:** added per-frame auto-rotate update inside the worldItems update loop — T3 `autoRotate:true` rotates `it.facing` at `rotateSpeedDegSec` (18°/s) when a Video Camera is mounted, giving an unattended panning sweep. **Sanity Meds:** R-key consume now uses tiered `maxUses` (T1=1, T2=2, T3=3) — T1 still single-shot single-bottle, T2/T3 track per-bottle uses through `itemCharges` so the same bottle can be consumed multiple times before it leaves the inventory. **Photo Camera:** `calculateContractMoneyReward`'s unique-photo bonus now multiplies by the active camera's `photoQuality` (T1=1.0, T2=1.25, T3=1.5); the breakdown row labels the multiplier so the player can see why their photos paid more. **Banshee target:** module-level `_bansheeTarget = player` already locked on assignment; threaded through the crucifix consumer's per-instance loop without changing the canonical `crucifix_always_works → Infinity` rule (single-player game so target == player always, but the helper is now plumbed for any future multi-target work). All three smoke suites still pass. Tier-2/3/4 work that is data-only (Sound Sensor T3 location pinpoint, ghost-specific deltas like Hantu cold-breath / Jinn breaker / Moroi smudge-blind / etc.) remains in the implementation plan as still-unwired.
- [x] 3-tier item / progression / money / shop system (data + plumbing pass). New SOT `EQUIPMENT_DATA` covers all 21 starter+optional items with `tier1/tier2/tier3` functional blocks, plus shared per-tier metadata (`unlockLevel`, `purchaseCost`, `tierImpl: full|partial|placeholder`, `art:{world,held,inventory,pegboard,journal}`, `shopBlurb`). Tier numerics follow current Phasmophobia v0.15.x canon (EMF: T2 distance / T3 direction; Spirit Box: T2 voice-activated / T3 wider listening cone; UV: T3 integrated white-light; Ghost Book: T3 indicator LED; Video Camera: T2 NV / T3 HD+NV; D.O.T.S.: wider cones; Thermometer: faster lerp + lower noise; Photo Camera: more shots + `photoQuality` payout multiplier; Sanity Meds: T2=2 doses / T3=3; Crucifix: T1=3 / T2=4 / T3=5 tile range; Tripod: T3 auto-rotate; Sound Sensor: T3 location pinpoint; etc). Existing 30+ `_itemTier1(name)` consumers automatically pick up the player's *equipped* tier via a redirect inside `_itemTier1` — so no shotgun edit was needed across gameplay code; `tierImpl:"full"` items get the new stats applied immediately, `partial` items consume the shared numerics but tier-specific *features* (UV white-light, EMF distance, SB voice-activation, Tripod auto-rotate, etc.) still need per-feature wiring. Player ownership + equipped tier persisted in `localStorage` (`_ownedTiers` / `_equippedTier`); helpers `getCurrentTier`, `_itemTier`, `isTierOwned`, `isTierUnlocked`, `setEquippedTier`, `grantTier`. Money system: `playerMoney` persistent, `addPlayerMoney` / `spendPlayerMoney` mutate-and-save, `calculateContractMoneyReward(result)` computes post-contract dollars (base accept + correct ghost ID + objective bonuses + unique-photo bonuses + difficulty mult + survival bonus), wired into `_endRound` so the cash banks immediately and the rows + total flow into the debrief data. Truck-monitor SHOP tab rebuilt: LVL 2 / LVL 3 panels are no longer "COMING SOON" — they show the tier-specific blurb, status (LOCKED / UNLOCKED / OWNED / EQUIPPED), price, and a context-aware action button (BUY $X / EQUIP / EQUIPPED / LOCKED — REACH LV N / INSUFFICIENT FUNDS) with click-to-buy and click-to-equip handlers, plus an `IMPL: full|partial|placeholder` badge per tier. Purchase / equip feedback rendered as a fading flash toast on the preview pane. Journal Items tab now shows a "Tier I/II/III" pill per equipment row (gilded for current tier) and reads the current tier's `shopBlurb` for the row description, so the journal stays in sync with whatever the player has equipped. All three smoke suites pass.
- [x] Phasmo-faithful journal rebuild. Goal: make Haunted's in-game journal feel like the current Phasmophobia journal — same tab structure, hierarchy, page flow, evidence narrowing, and ghost-page usefulness — based on April 2026 online sources (pcgamesn, dualshockers, keengamer, gamerant; the fandom wiki returned 403). All 27 ghosts now match canonical evidence. Three new SOT data tables added next to `GHOST_REGISTRY`: `JOURNAL_EVIDENCE_DATA` (per-evidence tool + tell), `JOURNAL_GHOST_NOTES` (per-ghost strengths/weaknesses/field-tip), `JOURNAL_SECTION_DATA` (the canonical 6-tab structure). Helper `_journalGhostData()` joins gameplay registry with notes; `_journalCandidateGhosts()` does Mimic-aware narrowing (false Ghost Orb does NOT eliminate the Mimic). Six tabs rebuilt: **Home** = "Field Manual" (Investigation / Sanity / Hunts) + "Hunt Survival" + live round stats; **Items** = "Equipment" grouped Evidence Gear / Support Gear with descriptions, plus "Cursed Possessions" + held loadout on the right; **Overview** = "Case File" header (Site / Size / Difficulty / Sanity / Guess) + Optional Objectives strikethrough on the left, Bonus-XP rollup on the right; **Media** = capture log on the left, per-category XP table + capture-bonus notes on the right; **Evidence** = 7 evidence types with three-state checkboxes + "→ tool" sub-label, suspect-narrowing column with "N of 27 still possible" counter; **Ghosts** = full detail card (Evidence / Strengths / Weaknesses / Field tip) on the left, 27-ghost click list on the right. Three Winter's-Jest ghost evidence sets corrected: Dayan was [EMF, Ghost Writing, Ghost Orb] → now [EMF, Spirit Box, Ghost Orb]; Gallu was [UV, Freezing, DOTS] → now [EMF, Spirit Box, UV]; Obambo was [Ghost Orb, Ghost Writing, Spirit Box] → now [Ghost Writing, UV, DOTS]. Their special-rule strings + behaviour numerics also updated to match canonical Winter's Jest behaviour (Dayan female-only / movement-provoke; Gallu enrage-on-protective-gear; Obambo calm/aggressive cycle). All three smoke suites pass (`node tests/run-smoke.js`). Sections fully aligned: Home, Items, Overview, Evidence, Ghosts. Sections partially aligned (limited by missing Haunted systems): Media (only photo captures wired — video / sound capture not yet in-game).
- [x] Phase B — Phasmophobia hunt-fidelity research → spec → plan → code. Active online-source pass to bring Haunted's hunt behaviour in line with current (April 2026, post-Winter's-Jest v0.15.x) Phasmophobia. Three-deliverable cycle: (1) `docs/PHAS_HUNT_MASTER_SPEC.md` already in place from the previous turn — canonical numerics: 50 % default hunt threshold, 25 s default hunt cooldown (Demon 20 s), flat 10 % per-roll probability, 1.7 m/s base hunt speed, +0.05× LOS accel/s capped at +0.65× over 13 s, −0.01×/s decay, 90 s default smudge prevent (Demon 60 s, Spirit 180 s, Yurei 90 s + room confine), crucifix tier ranges T1=3 m / T2=4 m / T3=5 m with Demon ×1.5, full per-ghost identity rule sheet for all 27 (post-Dec-16-2025 Dayan / Obambo / Gallu included). (2) `docs/HAUNTED_HUNT_IMPLEMENTATION_PLAN.md` written this session (~1,668 lines): Part 1 has 12 cross-cutting deltas (1.A hunt-roll formula, 1.B LOS accel cap, 1.C smudge per-ghost duration, 1.D crucifix tier function, 1.E hiding-spot ray cast, 1.F grace-period audit, 1.G hunt-end teardown, 1.H heartbeat/footstep audibility, 1.I electronics flicker, 1.J light-switch lockout, 1.K door behaviour, 1.L identity-flag inventory); Part 2 covers all 27 ghosts × 6 sub-sections (current Haunted hunt status / missing hunt-critical mechanics / dependencies / implementation order / risk-of-shallow / `__debug` hooks); Part 3 tiers the work T0 → T4; Part 4 lists known gaps (DOTS-camera-only, breaker-toggle prohibition, photo-evidence wiring, hidey-spot ray cast, target-only crucifix gating); Part 5 is the acceptance criteria. (3) Tier-0 + Tier-1 code now in `index.html`: `getHuntRollChance` rewritten to read flat 10 % from registry; Demon hard-coded threshold dropped from `getHuntThreshold` (uses behavior config 70 + `huntCooldownSeconds:20` + `smudgePreventSeconds:60` directly); LOS accel cap capped at 13 s / 1.65× with −0.2× decay; `_huntCooldownMult` neutralised to 1.0 (cooldown comes from behavior config, not multiplier); Onryo three-extinguish counter (`_onryoFlameTriggerCount`) force-triggers a hunt at ≥3 and resets on consume; Onryo flame-block range tightened from 180 px → 192 px (exact 4 m); Thaye threshold rewritten to use the canonical curve `[75,69,63,57,51,45,39,33,27,21,15]` capped at age 10 with the per-ghost 1/8 roll; Phantom blink period now reads `apparitionVisibleMs(Min/Max)` and `apparitionInvisibleMs(Min/Max)` from registry; Phantom's photographed-during-hunt branch now sets `_phantomPhotographedThisHunt` + drains 25 sanity (instead of vanishing the apparition); Polt hunt-throw barrage wired (4 s startup → 0.5 s interval → 4 m radius pickup target → 2 % sanity drain per throw); Wraith salt-immune consumer in ItemInteract (also covers Obambo's `skip_salt`); Shade same-room suppression (no hunt while ghost room equals player room or distance < 80 px); Twins fast/slow flag (`_huntTwinsFast`) re-rolled at every `_endHunt`; Obake six-finger fingerprint chance now reads `sixFingerFingerprintChance` from registry (default 1/6); registry numerics filled in for Demon / Phantom / Polt / Onryo / Yokai / Myling / Raiju / Obake / Thaye; new `__debug` hooks `huntIdentity()`, `onryoFlame()`, `phantomState()`, `poltState()` for runtime inspection. All three smoke suites still pass (`tests/run-smoke.js`). Tier-2/3/4 work documented in the plan but not yet coded — see SESSION_LOG entry for the still-blocked ghost list.
- [x] Defer debug "+ XP" grant until the player is back in the lobby. New `_pendingDebugXpFlow` slot queues the flow data when the grant fires while `gameRunning` is true; the corkboard branch of the main loop drains it on the very next frame the player is in the lobby (guarded by `!_postContract` so it never clobbers an in-progress round-end debrief). Mid-game grants no longer hijack live gameplay with the loading screen. Lobby-side grants still start the flow inline. Removed the in-game `drawPostContract` overlay added the previous turn — it was only there to mask the soft-lock and is dead code now that grants are deferred.
- [x] Post-contract flow reorder + paper-on-cork XP screen + debug XP soft-lock fix. New state order is `xpFill → (unlockCard ↔ xpFill) → debrief`, with `debrief` as the terminal screen — XP loading now pops up FIRST after a contract, debrief is the report you read on the way out. `startPostContractFlow` picks `xpFill` when `total > 0`, otherwise jumps straight to `debrief`. `advanceXpSequence` and `skipXpSequence` both land in `debrief` when XP runs out. `unlockCard` DONE routes back to `xpFill` while XP remains, else to `debrief`. `debrief`'s primary button is now always `"return to lobby →"` and calls `finishPostContractFlow` directly. `drawXpScreen` rebuilt to mirror the corkboard + pinned-paper aesthetic of `drawDebriefScreen`: same frame/border/woodgrain, red-tack title tag reading `PROGRESSION`, header strip `"Agent Progression"`, big 86 px brown level number, parchment levels-gained chip, gold XP bar with pulsing edge, dark-red level flash banner, italic applied-XP caption, parchment `"skip →"` button bottom-right of the paper. Debug "GIVE XP" button in the in-game HUD no longer soft-locks: main loop now also calls `advanceXpSequence` + `drawPostContract` inside the `gameRunning` branch (was corkboard-only), so the flow renders on top of live gameplay and dismisses cleanly without disturbing `gameRunning`.
- [x] Van controls fix. The van keypad popup ("VAN CONTROL") is now the only way to open or close the van's rear door — the E-key facing toggle ([index.html:3874-3875](index.html#L3874)) and the click-to-toggle ([index.html:3964-3965](index.html#L3964)) on the door body were both removed. Pressing the keypad's "Open Van Doors" button now opens the door AND auto-closes the popup. Pressing "Leave Site" auto-closes the popup, closes the van door, captures the player's guess result, and starts a 5-second countdown (`_leaveSiteTimer`) that fires `_endRound(false)` when it expires. A "LEAVING SITE — loading out in N s" banner renders top-center while the timer counts down so the player gets visible feedback during the wait. Guarded against double-presses via `_leaveSiteTimer <= 0` precondition.
- [x] Phasmo-style contract XP system. Replaced the old inline XP construction in `_endRound` with a data-driven pipeline of named pieces (`ghostIdXp`, `objectiveXp`, `mediaXp`, `investigationBonusXp`, `perfectBonusXp`, `boneXp`, `weatherBonusXp`, `subtotalBeforeDifficulty`, `difficultyAdjustedXp`, `deathAdjustedXp`, `finalXpAwarded`) computed by `calculateContractXpBreakdown(result)`. Tables: `MAP_SIZE_REWARDS` (Small/Medium/Large → ghostId + bone), `OBJECTIVE_DEFAULT_XP=25`, `OBJECTIVE_HIGH_VALUE_XP=30` (id set: use_incense, incense_cleanse, firelight_blowout), `MEDIA_REWARDS` (photo/video/sound × category → base/unique/duplicate), `MEDIA_EQUIP_TIER_BONUS` (1→0, 2→+1, 3→+2), `PHOTO_CATEGORY_MAP` (Haunted's photo type strings → canonical Phasmo categories), `WEATHER_XP_BONUS = { blood_moon: 0.10 }`, `_DEFAULT_DIFF_TIERS = [1,2,3,4,6]` for Custom round-down. Functions wired: `calculateGhostIdXp`, `calculateObjectiveXp`, `calculateMediaXp`, `calculateInvestigationBonusXp`, `calculatePerfectBonusXp`, `calculateBoneXp`, `applyDifficultyMultiplier`, `applyDeathRule`, plus the orchestrator. Order of ops: `subtotal = ghostId + objectives + media + investigationBonus + perfectBonus` → apply difficulty mult to `subtotal` (custom rounds down to nearest default tier on a wrong guess) → `+ bone` (excluded from diff mult) → `+ floor((subtotal+bone) × weatherBonus)` → death rule (strip diff bonus, halve the rest) → floor → `finalXpAwarded`. MAP_REGISTRY now carries a `size` field. Wired: ghost ID, objectives, bone (via photograph), investigation bonus, perfect bonus structure, difficulty mults, custom wrong-ghost rule, death rule, rounding. Placeholder (awards 0 today): video media (no capture), sound media (no capture), Tier II/III equip bonus (no tier data), Blood Moon weather (`weather:"clear"` hardcoded — no env mode for it). Debrief table reads the breakdown's `rows[]` directly, so the existing renderer surfaces every component correctly. `npm run syntax` + `npm run test:smoke` clean.
- [x] Post-contract flow rebuild — Phasmo-style four-phase presentation. Old `_resultsPopup` + `_xpSeq` replaced by a single `_postContract` state machine with states `"debrief" → "xpFill" ↔ "unlockCard" → "complete"`. `_endRound` now calls `buildContractResultData(died, total, rows, _xpSnap)` then `startPostContractFlow(data)`; the flow renders an atmospheric paper/corkboard debrief FIRST (XP table + outcome line + Map / Difficulty / Ghost / Guess / Sanity / Photos / Evidence / Objectives — restrained even on death), then a clean focused XP-fill screen with a big level number + wide gold bar + per-level flash banner + Skip button, then per-level grouped unlock cards (icon + label + AVAILABLE NOW/COMING SOON + DONE button + click-anywhere-on-card to advance + skip-all option), then a final summary with total XP / levels gained / Return to Lobby. Skip jumps to the final summary without losing rewards (real progression already applied by `awardXp` before the flow runs — display cursor is a visual replay). `levelFillTime` clamps to `[0.6, 1.0]` s/level so even a 50-level dump walks through every level visibly. Renderers: `drawPostContract` dispatcher → `drawDebriefScreen / drawXpScreen / drawUnlockCard / drawCompleteScreen`. Click handler routes on `_postContract.state`. Debug XP grant button now drives the same flow with synthesized "Debug Console" debrief fields, so the whole presentation is testable in one click. `_resultsPopup` kept as an alias for any remaining "is a popup open?" checks; legacy `_xpSeq*` aliases stay null.
- [x] Level-up sequence: every level cross now pauses on a popup card (not just unlock levels), debug XP grants animate the same as round-end, and pace floor raised so each level fills visibly. Empty levels show "LEVEL N REACHED — No new unlocks at this level"; unlock levels show "LEVEL N UNLOCKED — <icon + label + AVAILABLE NOW/COMING SOON>". `_xpSeqDoneRect` widened to the entire card so clicking anywhere on the popup advances (rapid-fire path for grinding through many levels). `levelFillTime` clamp raised to `[0.5, 0.9]` s. Debug XP grant button at `index.html:15012` now builds an `_xpSnap`, calls `startLevelUpSequence`, and synthesizes a minimal `_resultsPopup` so the combined debriefing UI hosts the animation — making debug XP testing visually identical to a real round.
- [x] Level-up animation now paces PER LEVEL instead of per total XP. Replaced `fillSpeed = clamp(80, 8000, totalEarned / 2.5)` with `levelFillTime` (seconds per level, clamped 0.25–0.6 s based on `_approxLevels`); fill rate is `displayNeeded / levelFillTime` recomputed each cross, so every level takes the same visible time regardless of XP cost. Added a `levelUpFlash` (0.45 s timer) + `flashLevel` to `_xpSeq`: every level cross — even ones with no unlocks — sets the timer and `drawResultsPopup` renders a "LEVEL N!" banner in the XP bar's right margin with an in/out alpha fade. Empty-level crossings flash and continue (no popup); only levels in `unlocksByLvl` flip `phase = "popup"`. Result: a 20- or 100-level dump visibly walks through every level instead of finishing in a flash, and the player only has to click DONE on actual unlock levels. SKIP still snaps to end.
- [x] Combined debriefing + progression into one unified popup. The level-up animation no longer fires *after* the debriefing closes — it now runs *inside* the debriefing. `_endRound` calls `startLevelUpSequence(_xpSnap)` immediately when `total > 0`, so the moment the debriefing appears, its XP bar starts filling live. `drawResultsPopup` reads `_xpSeq.displayLevel / displayXp / displayNeeded` instead of the static post-mission values; a pulsing edge marker on the bar signals it's animating. The bottom button is now context-sensitive: "skip →" while filling (calls `skipLevelUpSequence`, which snaps to end and nulls the sequence), hidden while a level-up unlock card is up, "continue →" once the sequence finishes (closes debriefing and advances to the Investigator page). On every level threshold, `_xpSeqStep` pauses the fill (`phase = "popup"`) and `drawXpSequence` overlays a centered unlock card with that level's unlocks + a DONE button — so a 20-level gain gets 20 popups, one per level, not just a jump to the highest. DONE on the card resumes the fill if XP remains, or auto-finishes (`_xpSeq = null`) if not. `drawXpSequence` was stripped down to just the overlay card; the old separate "AGENT PROGRESSION" full-screen card and the "summary" phase are gone — the debriefing's existing payment table + XP bar serves as the unified surface.
- [x] Mode-select popup redesigned into a cleaner Phasmophobia-style 2-column layout. `drawMenuModeSelect()` body rewritten: LEFT column (~67 % of inner width) has a 34 px "INVESTIGATION TYPE / Choose your contract." header strip with a 4 px gold accent stripe, then 3 large mode cards stacked vertically (SINGLE PLAYER, MULTIPLAYER, TRAINING) sized as `floor((contentBot - cardsTop - CARD_GAP*2) / 3)` so they fill the column evenly. Each card shows a larger illustration (~180 × ph-24), a 20 px Georgia-bold title with a gold underline accent, italic 12 px sub, italic 10 px detail, and a 120 × 40 SELECT pill on the right edge; Multiplayer + Training stay LOCKED. RIGHT column (~33 %) keeps the "AGENT FILE" presentation as a 156 px card on top (ID, level via `getLevelDisplay`, rank line, full-width XP bar with gradient + threshold ticks), with a separate PRESTIGE panel directly below as its own 92 px dark card with a red accent stripe, "PRESTIGE" header, current tier sub-label, and an "ASCEND → roman" / "REACH L100 — N TO GO" / "MAX PRESTIGE (XX)" gated button. Click contracts preserved: `_modeSelectRects.push({ id, x, y, w, h })` for each mode card and `_modeSelectPrestigeRect = { x, y, w, h }` for the prestige button.
- [x] Post-mission progression flow + Haunted-only map unlocks: trimmed `LEVEL_UNLOCKS` to drop fake Phasmophobia map names, added a single new map gate at L15 (`map:gallows_inn`, wired:true) since 1st Roseboosh is the L1 starter and slots 2/3 in `MAP_REGISTRY` are still placeholders. `MAP_REGISTRY` got a `levelReq` field + `isMapAvailable(map)` helper so the corkboard map-select shows Gallow's Inn as a level-locked card ("LVL 15" badge) until the player levels up. New post-mission XP-fill / level-up animation: `startLevelUpSequence(snap)`, `_xpSeqStep(delta)`, `skipLevelUpSequence`, `finishLevelUpSequence` with phases `fill → popup → summary`. Triggered from "continue →" on the results popup; XP bar animates from pre-mission state, pauses on each level threshold to show an unlock card with name + procedural icon (map=house, equipment=toolbox, difficulty=star, prestige=crown) + Done button; Skip button jumps straight to the summary; final Done closes. Real progression is applied by `awardXp()` before the sequence — the animation is a visual replay, so skip preserves correctness.
- [x] Progression / level system rebuild: Phasmophobia-style XP curve (`floor(100 * (L-1)^1.73)` for 1–100, flat 4,971/level for 101–999, `floor(4,468,929 + 100 * (L-900)^1.73)` for 1000+) with single-source-of-truth `LEVEL_UNLOCKS` data table covering maps, difficulties, equipment Tier 1–3, and a Level-100 prestige flag. Helpers `getCumulativeXpForLevel / getXpToNextLevel / getUnlocksForLevel / applyLevelUnlocks / awardXp / getNextUnlockMilestone / getRankForLevel` replace the old `* 1.3` stub. `_unlockedSet` preserves granted state. Results popup shows newly unlocked entries (or next-milestone teaser + prestige flag); title-screen profile card shows the next milestone under the XP bar and uses a data-driven rank. Tier-2/3 + new map names + prestige are data-only (`wired:false`); Tier-1 equipment + Amateur/Intermediate/Professional/Nightmare/Insanity/Custom difficulties are wired and exist in code but ungated this task.
- [x] Whisper system rewrite: removed random auto-hotspot spawns and auto-pickup. WHISPER HERE button now lures the ghost to the player's spot — clears `ghostPath`, points `ghostFavRoom` at the player's room, holds `ghostTargetX/Y` on the lure point each frame, and plays the whisper when ghost arrives (within 18 px) or after a 12 s timeout. Gate: holding Sound Recorder + recorder is on + same room as ghost + not hunting + no lure already running. Disabled labels tell you which condition failed.
- [x] Player sprite empty-hands: held-item-on-player render gated by `_heldActive` so passive carries (crucifix, salt, bone, tarot, meds…) no longer appear glued to the character. Active items (flashlight on, lighter lit, powered electronics) still render in hand.
- [x] Ghost whisper hotspots: 5 source clips trimmed to ≤5 s with proportional fades (short = 100/250 ms, long 5 s = 500/800 ms), saved as `audio/whisper-1..5.m4a`. Per-round seed of 3 hotspots in random interior rooms (skip Yard), each with a random whisper index + 32 px radius. Pickup only fires while holding an active Sound Recorder; 6 s cooldown between whispers, purple "WHISPER" HUD banner on play.
- [x] Sanity Meds SFX: trimmed the source pill-bottle shake audio (8.6 s, multi-shake) down to the first shake only — 0.080–0.500 s window, 5 ms fade-in / 15 ms fade-out. Saved as `audio/sanity-meds.m4a`. New `sndSanityMeds()` fires once on R-key consume.
- [x] Bottom-right held-item HUD locked: removed the walking hand-bob + wrist-roll sway so the item stays in a fixed position.
- [x] CCTV truck-monitor: full-detail view at the camera's POV (items, ghost, candle flame + burnout, D.O.T.S. dots, hunt flicker, player sprite). Old simplified `renderCameraFeed` replaced by reusing `draw()` with `_cctvViewActive` view-origin override; camera-cone fog + NV tint applied via early return after the world transform.
- [x] Lobby music playlist: 3-track random rotation (`lobby-music.mp3` + 2 new ones moved from project root and renamed `lobby-music-2/3.mp3`). Random pick on start, `ended` listener auto-advances with slight no-immediate-repeat bias. Volume + stop now apply to all tracks.
- [x] Ghost search bias: `_pickNextSearchRoom` weights candidates by `1 / (1 + visits)` so already-searched rooms become progressively less likely; `_huntSearchVisits` map resets once all rooms in `roomGraph` have been visited at least once (re-seeded with the ghost's current room). Wired into warning + active-hunt rail picks; cleared in `_initHuntLastKnown` and `_endHunt`. Debug HUD shows top 6 `room×count` entries.
- [x] Polish pass 3: sprint dust trail (3 phased ellipses behind player when sprinting indoors, peaking at α 0.32 → fade as they sprawl outward)
- [x] Polish pass 2: ghost ground shadow under sprite (anchors to floor, scales alpha with sprite alpha); proximity-based hunt camera shake (`_huntFlashDim²` quadratic ramp during active hunts, flat 0.35 baseline during warning) layered on top of existing `_deathShake`; door-slam camera jolt (`_huntSlamJolt` bumps to 1.0 inside `sndDoorSlam` if hunting/warning, decays at 4/s, stacks +2.4× into the shake amount)
- [x] Ghost rails when no LOS: `_buildRailPath` inserts intermediate room centres so search / lastSeen / warning all traverse `doorway → roomCentre → doorway → roomCentre`; killed `_huntSearchX/Y/Timer` random wander; ghost stops at room middles, picks next target there; LOS still overrides via chase
- [x] Journal tab page-flip tuning: reverse bias to 30 % A / 70 % B (big sample is now primary); both shortened to ~200/180 ms quick burst so rapid tab-switching doesn't lag behind a slow turn; every click hard-restarts both elements so a new click cleanly cancels any in-flight sample
- [x] Hunt chase LOS-only beeline: drop same-room constraint, beeline whenever `_hasLOS`; path updates continuously while LOS holds, freezes when LOS lost; LOS rules same as player's
- [x] Light switch samples: `audio/light-on.mp3` (200ms trim) and `audio/light-off.mp3` (80ms trim) wired into all 4 toggle paths (player E, player click, ghost debug, ghost AI); `sndLightSwitch(isOn)` dispatcher routes by post-flip state
- [x] Journal tab: two page-flip samples (A small / B big), trimmed to detected sound boundaries via -40 dB-rel-peak silence threshold; A plays full 740ms, B clipped to 400ms; random non-repeating alternation
- [x] UI click samples: `audio/journal-tab.mp3` for journal tab switches (only when tab actually changes); `audio/monitor-click.mp3` for any click on the pre-game lobby monitor (corkboard); both use cloneNode for overlap-safe rapid clicks and route through SFX/Master sliders
- [x] Sample-based interior footsteps: 3 footstep samples (`audio/footstep-1.mp3`–`footstep-3.mp3`) with non-repeating random pick, sprint-aware volume (0.55→0.80), and 0.93–1.07 playbackRate wobble; gated to wood + tile surfaces only — concrete/grass/van still use old synth pending future samples
- [x] Van engine idle ambient: `audio/van-idle.mp3` looping; positional fade outside (tight 90 px range so crickets stay dominant), muffled low-pass (600 Hz) inside van for inside-vehicle feel
- [x] Cursor style setting: persistent Open/Closed toggle in pause menu (default Closed); gates both the cursor hide and the on-canvas reach hand
- [x] Reach hand indicator: hides OS cursor in active gameplay, draws a small pixel hand at 30 world-px reach (rotates with playerAngle); cursor restored when journal/pegboard/cork/camera/keypad/menu open
- [x] N key conflict fixed: debug `_dbgNoDetect` toggle now skipped when Video Camera is in active slot, so N falls through to camera NV
- [x] Handheld Video Camera NV: dark-room overlay bypassed when held + NV on; cone fog punch holds full erase to 55% range; green phosphor tint + `lighter`-mode brightness boost inside cone — dark rooms now visibly readable through camera
- [x] Pegboard item visuals: hooked into `_drawItemArtCentered` so pegboard, ground drop, hand, HUD icons share one detailed art switch
- [x] Held item next to player: replaced primitives with `_drawItemArtCentered`, with per-facing translate/rotate/scale (down/up/right/left)
- [x] Fix ghost hunt door traversal: hunting ghost ignores door collision, opens doors on contact with slam sound + UV prints; canGhostMoveTo skips door check when ghostHunting/huntWarning
- [x] Fix ghost van/yard safe zone: ghost no longer targets player in van or yard during hunts; 7 target acquisition pathways gated by _playerInSafeZone
- [x] Fix ghost doorway traversal: _ghostRoom() helper resolves doorway positions, waypoint threshold 8→14px, hunt chase stuck recovery added
- [x] Fix ghost doorway threshold jitter: hunt collision reduced to 4×4 in canGhostMoveTo so ghost crosses room-doorway boundaries without hesitation
- [x] Add hunt/search debug HUD: Search line (ghost room → target room, search count, path remaining) + State line (LOS, hidden, last known room, player room)
- [x] Split debug HUD into 3 panels (Ghost Info / Hunt Debug / Collapsible Controls); controls collapse via click or T key
- [x] Fix pre-hunt hidden players unfairly found: _initHuntLastKnown() now checks isHiding before seeding ghost with player position; affects 6 cursed hunt starts
- [x] Rebuild hunt detection rules: remove omniscient tracking from warning phase, range-gate noise (200px) and electronics (150px), kill requires active detection window, warning→hunt carries over detection state instead of re-reading player position
- [x] Add debug no-detection toggle: [N] key or button disables all ghost sensing (LOS, noise, electronics, closet reveal, furniture spot-check, hunt start seeding, kill check)
- [x] Add debug lights ON/OFF buttons: side-by-side in debug controls panel; ON flips breaker + all switches, OFF turns all switches off
- [x] Fix ghost hunt doorway traversal: same-room-only LOS beeline (cross-room uses BFS waypoints), multi-corner walkable fallback during hunts, warning phase same fix, debug waypoint overlay + GHOST→NEXT ROOM button
- [x] Add committed doorway-crossing state: 3-phase crossing (approach→cross→exit) prevents ghost oscillation at doorway thresholds; unrestricted movement within doorway rect during crossing; applied to both hunt + warning phases; debug overlay + HUD for crossing state
- [x] Cinematic jumpscare death animation: 5-phase kill sequence (impact→lunge→face→collapse→blackout), 3 random variants (lunge/flicker/static), phase-based shake, layered audio, ghost sprite scales toward camera with horror filters
- [x] Fix centered doorway crossing: _computeCrossingPoints helper determines axis-aligned approach/cross/exit points; ghost lines up 14px before doorway center then crosses straight through; collision bypass expanded 4px around doorway rect; applied to both warning + active hunt
- [x] Post-death ghost phase: 5s spectral respawn at death location with journal access for ghost guess, then 2s loading screen, then lobby with results; immobile player, spectral rendering, dark vignette overlay, hunt-blocked
- [x] Refine post-death flow: fix journal timer freeze (tick moved to main loop), strip all death text from ghost phase overlay, cinematic extraction loading screen matching load-in style (PRU header, "Leaving site", maroon progress bar, 2.5s duration)
- [x] Remove death message screen: stripped "ELIMINATED" and "the ghost got you" from blackout phase; journal already opens normally during post-death (no blockers found)
- [x] Fix post-death journal + ghost movement: DEBUG_GHOST_BEHAVIOR block was intercepting J key before journal handler; added postDeathPhase guard to skip debug keys while dead; enabled 55% speed floaty movement during ghost phase (desktop + mobile)
- [x] Dedicated sanity HUD: top-left pill below difficulty badge with SANITY label, percentage readout, bar meter, 3-color tiers (green/amber/red), low-sanity flicker+flash+glow effects
- [x] Sanity breakdown panel: click-to-expand live debug showing net rate, 4 continuous factors (passive/ghost sight/hunt/sprint), power+difficulty multipliers, recent instant hits with 6s fade; _sanTrk tracker instrumented across drainSanity + 7 setSanity call sites
- [x] Sanity HUD readability redesign: wider pill (136×42) with status word + rate on main view; breakdown grouped into Environment/Movement/Ghost/Modifiers/Recent sections with On/Off labels, friendly names, color-coded active vs inactive rows
- [x] Baseline sanity rules alignment: DARK 0.06→0.12, LIT 0.02→0.00, sprint 0.06→0.08, hunt drain removed, ghost event 3→5, spirit box 1.5→3, EMF5 1→2, writing 2→3, ouija question 2→5, ouija break 4→10, meds 40→35; HUD redesigned with exact baseline categories/thresholds
- [x] Difficulty sanity rules: multiplier (x0.85��x1.45) moved from instant hits to continuous drains only; presets corrected (sanityDrainSpeed 85/100/115/130/145, Insanity startingSanity 75→100); HUD Difficulty section shows name+stars, drain mult, setup protection, hunt protection
- [x] Fix environment sanity detection: _roomAtPoint→_ghostRoom (doorway-aware) as single source of truth; detection runs every frame; HUD shows Room/Light/Power/Status/Drain diagnostic; flashlight excluded from room-light check
- [x] Fix hunt chase vs search: explicit _huntState state machine (chase→lastSeen→search); ghost only beelines with LOS; moves to exact last-seen position after LOS breaks; search at 55% base speed; noise/electronics redirect search but don't restore chase; debug HUD shows state+LOS+last seen pos
- [x] Hunt LOS debug + slow-hunt test mode: prominent center-screen LOS badge (green/red), thicker LOS line, SLOW HUNT toggle [S] (25% speed), Player Room in hunt HUD, slow-hunt indicator in HUD + badge
- [x] Phasmophobia-accurate ghost baseline hunt speeds: all 24 ghosts set via ratio conversion (phasSpeed/1.6 × player.speed); standard=1.0625×, special baselines for Revenant/Deogen/Hantu/Moroi/Thaye/Twins/Dayan/Gallu/Obambo; ramp+difficulty modifiers moved after all special overrides; Jinn burst=1.5625×, Raiju electronics=1.5625×
- [x] Debug see-ghost-through-walls toggle: [G] key or GHOST VIS button in debug controls; cyan-tinted silhouette drawn AFTER fog stamp so never occluded; labeled "GHOST" marker; debug-render only, no AI/LOS/pathing/detection changes
- [x] L-shaped storage bin hiding spots: solidObstacles[] array with 4 L-shaped crate formations (Garage/Utility/Living Room/Bedroom 2); blocks player+ghost movement via canMoveTo/canGhostMoveTo, blocks LOS via _huntHasLOS; player physically walks behind bins to hide; ghost respects geometry naturally — no special-case AI needed
- [x] Ghost flash event: pale rect appears/fades at random floor positions after safe time
- [x] Roaming ghost entity (bounces diagonally, visible after safe time)
- [x] House shell layout: 9 rooms placed and aligned (edges snap cleanly)
- [x] Redesigned house layout: 12 rooms, hallway spine, foyer, garage/laundry, master suite, kids wing, kitchen
- [x] Connect rooms with doorway walkable rects (11 open-gap connectors)
- [x] Wall collision fix: canMoveTo now requires full player containment inside one walkable rect
- [x] Verify doorway traversal with debug overlay, then remove DEBUG_COLLISION flag
- [x] House shell fill: wall gaps now read as wall material (dark warm brown #2a2018)
- [x] Scale house layout 1.5x: all room/doorway/spawn/exit/ghost-bound coords multiplied by 1.5
- [x] Camera-follow system: world drawn with ctx.translate offset; HUD stays screen-fixed
- [x] Visual wall system: walls[] array with 9 segments drawn before rooms (visual only, no collision change)
- [x] Closet/storage spaces: 6 new walkable rooms + 5 connectors; kids rooms shrunk h:180→h:120
- [x] Closet/storage refinement: kids closets shrunk, garage storage isolated with one entrance, house shell removed
- [x] Layout refinement: walk-in snapped, kitchen extended south, kids closets flush-left, walls updated
- [x] House layout redesign: original layout inspired by suburban horror feel — 14 rooms, new flow (Foyer→Hallway→wings), master chain, garage via utility, Study dead-end, coat closet
- [x] Outside area: front yard, driveway, street, van; foyer+garage exit via connectors; exit zone moved to van
- [x] Outside area expanded: single 700×200 rect, yellow boundary tape on 3 edges, street/van pushed south
- [x] Outside zones refined: grass, walkway, driveway, road drawn as visual layers; garage entrance removed
- [x] Van enlarged to 180×80px; outside area deepened to h:220; road to 100px; van positioned for future interior use
- [x] Van made enterable from back only: outside split into Yard/RoadLeft/RoadRight/VanInterior; van body solid on all other sides
- [x] Van entrance moved to east/right side (full height); van drawn over yellow tape; bottom tape removed
- [x] Van collision: 3-wall system (north/south/west solid, east open); player can enter through back doors
- [x] Interior visual pass: room labels removed; furniture added to all main rooms (top-down blocks)
- [x] Lighting/shading pass: NW light direction, wall edge shading, floor textures, furniture shadows, detail accents
- [x] Color pass: natural home tones for floors, walls, furniture, outside zones
- [x] Door system: 17 doors with open/close toggle (E key), wood visual, closed doors block movement
- [x] Visibility system: doorway wedge FOV; player sees current room fully + triangle through each open door; closed doors block sight
- [x] Sight range: radial gradient falloff on fog canvas; clear within 90px, soft fade to 260px, near-dark at edge
- [x] Room lights + flashlight: E key toggles switches, F key toggles flashlight; 40° cone, 220px range; unlit rooms dark
- [x] 360-degree mouse aiming: playerAngle updates from mousemove; flashlight and FOV follow cursor
- [x] Fix gray-screen bug: camX/camY were undefined in update(); now computed locally with _camX/_camY
- [x] Player sprite rotates to face mouse: body + front highlight + nose dot rotate with playerAngle
- [x] Person sprite + walk animation: head/body/legs drawn with sin-based leg swing while moving
- [x] Van equipment wall: pegboard screen (view-only) with 6 items; E to open, ESC to close
- [x] Van visual pass: top-down redesign (shadow, roof cutaway, cab, monitors, wheels); pegboard south wall; tool silhouettes improved
- [x] Outside night + roof: dark overlay, front-door warm glow, roof shows from outside, hides inside
- [x] Roof detail pass: wall faces, 4 sections, shingles, ridges, valleys, eaves, gutters, chimney
- [x] Environment modes (M key): night, nightrain, sunrise, sunset, bloodmoon, bloodmoonrain; rain particles; roof circle fog-clear
- [x] Gameplay zoom (ZOOM=1.8): world + fog canvas scale; sight/glow radii adjusted; mouse angle simplified
- [x] ENV mode visual pass: fogColor, skyGlow added; stronger tints; roof-reveal circle uses soft gradient fade
- [x] Directional lighting: sunrise east-gradient+bloom, sunset west-gradient+bloom, blood moon overhead radial
- [x] Env mode: random on start, M key removed, all intensities toned down
- [x] Sunlight physically limited: world-space rects only (outside+foyer+van); fog blocks interior; skyGlow removed
- [x] Sunlight refined: outside wash reduced (0.10 peak), foyer leak gated on front door open, van ambient bleed stays; no deep interior penetration
- [x] Sunlight as directional openings: closed door = zero leak; open door = clipped radial spill from threshold only; van same (gated on vanDoor.open)
- [x] Weather/light leakage fix: rain clipped to outside rect; blood moon + nightrain tints moved to world-space only (no interior bleed); sunrise/sunset outside alpha raised
- [x] Rain on roof + road: when playerOutside clip expands to y:60–982 (roof + outside); when inside stays y:720–982 only
- [x] Rain draw-order fix: moved rain block to after road/tape draw so streaks appear on top of asphalt; sunrise/sunset alpha raised to 0.36 peak
- [x] Pegboard redesign: one wide virtual board (1400px), single item row, visible hanging pegs, ←/→ scroll (max 560px), scroll indicator bar
- [x] Pegboard expanded: 11 items with qty field; 5 new silhouettes (Sound Recorder, Sanity Meds, Lighter, Incense, Crucifix); qty badge per slot; qty=0 shows empty dark slot; 16 total slots (3900px board)
- [x] Pegboard 3-row layout: 4 items/row × 3 rows (rowEy 8/154/300), 6 cols × 3 rows = 18 slots, 1260px virtual board; short peg nubs per row; cards 160×130px
- [x] Pegboard zoom removed; gameplay zoom added: gameZoom variable (0.8–3.0, default 1.8); +/- keys during play; zoom% shown in HUD
- [x] Fix van/movement crash: `const mode` used before declaration (ReferenceError); moved to top of draw()
- [x] Inventory/pickup system: click pegboard to take items (max 3 + lighter), Q cycle, HUD slots, held-item visual, flashlight requires pickup
- [x] Ghost roster: 24 ghosts with exact canonical evidence combos; canonical 7 evidence names; The Mimic fake-orb note
- [x] Journal tabbed upgrade: Evidence tab (checkable evidence types + narrowing) + Ghost Guide tab (entries per ghost with notes + evidence)
- [x] Ghost roster expanded to 6 with evidence[] and notes fields
- [x] Journal visual redesign: aged paper, leather spine, ruled lines, serif font, strikethrough ruled-out ghosts
- [x] World item drop/place/pickup: G places selected item in front of player; E picks up nearby world items; worldItems[] array; item label + color dot drawn in world
- [x] One real ghost per round: currentGhost chosen from 24-ghost roster; invisible to player; position tracked internally
- [x] Round-end flow: journal ghost selection (★ click), van keypad (Open Doors / Leave Site), result screen shows guess vs actual
- [x] Remove old prototype systems: ghostFlash, notebook, photo, exit zone, old guess screen, guessing state var
- [x] Journal evidence 3-state cycling: neutral / confirmed / crossed out
- [x] Journal ghost list 3-state: selected guess / crossed out / neutral (left/right click)
- [x] Journal redesigned for single-click interaction: separate confirm/cross-out controls, no right-click needed
- [x] Journal single-click cycles: evidence (neutral→confirmed→crossed→neutral), ghost (neutral→guess→crossed→neutral)
- [x] Journal redesigned as open two-page book spread with center crease
- [x] Journal Options tab: sensitivity control + keybind rebinding system
- [x] Fixed keybind rebinding: rw missing from click handler caused silent NaN hit-test failure
- [x] Sprint mechanic: hold Shift (rebindable) to move at 1.6× speed
- [x] Flashlight sensitivity: lerp rate on playerAngle, adjustable in Options tab
- [x] Journal ghost tab: always exactly one guess selected (defaults to Spirit)
- [x] Evidence narrowing: confirmed = must match, crossed = must not match
- [x] Tool placement system: worldItems store facing+active; R key toggles; per-item visuals
- [x] D.O.T.S. Projector: added to equipment, dot-grid field when active
- [x] Flashlight placed: beam cone drawn when active
- [x] Sound Recorder placed: pulse ring animation when active
- [x] Video Camera placed: facing arrow + red recording dot when active
- [x] Lighter: lit/unlit state (lighterLit), bright flame glow when lit
- [x] Fix pegboard crash: D.O.T.S. shape missing from shapes[] caused TypeError, leaked ctx.save, corrupted rendering
- [x] Per-item top-down floor art: all 13 placed tools have detailed ctx-drawn visuals (no more colored squares)
- [x] Player visual upgrade: arms, shoes, ears, hair, enlarged shadow; right arm stiffens when holding item
- [x] Held-item mini art: 10 items have custom in-hand visuals at right-hand position; lighter has flame when lit
- [x] Held-item scale + state visuals: items 1.6× larger in hand; Flashlight/UV on=glow, Lighter lit=flame halo; others have TODO placeholders for held-state wiring
- [x] Screen-fixed enlarged held-item panel: bottom-right (698,462), 92×92px, 3.5× scale, state-aware, zoom-independent; floor+hand items rescaled to 0.5× player
- [x] Held-item activation system: heldItemOn toggle (R), heldItemUsing hold-to-use (R held); Sound Recorder 3-state visual; D.O.T.S./Camera/Incense wired; panel box removed
- [x] Ghost evidence system (3 types): EMF Level 5 (meter on reader), D.O.T.S. figure in field, Spirit Box response overlay; ghost has position+favorite room; only matching ghosts trigger each evidence
- [x] Placed items scaled 0.75× so they read as smaller than the player on the floor
- [x] Q key lighter cycling: lighter reachable as 4th slot; HUD slot highlights + shows [LIT] label when selected
- [x] Placed tool active state preserved: items placed while on stay active in world (active: heldItemOn)
- [x] D.O.T.S. refinement: denser smaller grid (49 dots, 1px radius, 15px row spacing, 9px col spread)
- [x] Active placed-tool world visuals: Flashlight gradient cone + lens dot; UV Flashlight purple cone; Sound Recorder dual pulse rings + REC dot; Video Camera blue FOV fan + blinking REC dot; Incense orange warm glow
- [x] Held lighter glow: small orange radial gradient around player when lighter held and lit (r=55)
- [x] Lighter fog visibility: lighter held+lit punches soft radial clear in fog (r=65); only when lighter slot selected
- [x] UV flashlight separated: own uvFlashlightOn state; range 180 (vs 300), 55% partial erasure; only reveals when held
- [x] Debug ghost (DEBUG_GHOST flag): Ultimate Ghost always in Foyer, all evidence types, fires every 2s at 100% — disable by setting DEBUG_GHOST=false
- [x] Ghost visible appearance: decayed player — same proportions, pale grey-green skin, sunken dark eye sockets, grey iris glow, cheek rot, forehead gash, limp arms, bare feet
- [x] Ghost natural movement: waypoint walking (speed 1.4px/frame), 0.6–2.2s pauses, faces travel direction, leg/arm walk animation; no teleport drift
- [x] Ghost collision: canGhostMoveTo() — blocks closed doors, walkable-rect containment, optional room confinement; wall-sliding + fallback target on full block
- [x] Master ITEMS registry: 28 items (9 starter, 12 optional, 7 cursed); itemColors + directionalTools + equipmentList all derived from it; pegboard auto-scales; stub fallback shape prevents crashes
- [x] D.O.T.S. fully implemented: field cone geometry check (getDotsProjectorAtGhost), ghost visible only in field during disturb state, green-tinted body with scan-lines and flickering eye glow, natural walk animation preserved
- [x] Flashlight fog gated on held: cone only clears fog when Flashlight is selected slot
- [x] Camera furniture placement: hold-G preview system; furniture[] array; nightstand in foyer; ghost preview (blue=valid, red=invalid); direction arrow; release G commits; angle follows mouse; no floor placement
- [x] Tripod basics: place/pickup/carry; mountedCamera slot; attach Video Camera via E; pickup returns both; floor art (3-leg top-down, bigger than other items, camera block when mounted); held art (folded column); smart E prompt
- [x] Spirit Box: R key starts 2.5s listen window; response checks same-room+range+lights-off+evidence+chance; 3 overlays: listening (pulsing dots), no response (grey dashes), response (cyan); hand art pulses when listening
- [x] Thermometer: roomTemps[] per-room °C; ghost cools its room (6°C/s debug, 1°C/s normal); freezing evidence → −10°C floor, else 5°C floor; warm back to 20°C when ghost leaves; held readout with color coding (white/cold/freezing); hand art shows live temp
- [x] UV Light (Ultraviolet): ghost leaves fingerprint marks on nearby doors; uvMarks[] persist per round; markInUVCone() checks held+placed UV cones; marks drawn in world with purple glow+ridge art only when illuminated
- [x] Ghost Writing: placed active Ghost Book within 120px gets written by compatible ghost; written state persists; 1.8s shake+glow animation on write; EMF event spawns at book location; purple indicator dot; closed book shows cover, open blank shows ruled lines, written shows ink scrawls
- [x] EMF Reader: location-based sources (emfSources[]); ghost interactions push {x,y,level,timer}; EMF5 ghosts 30% chance of level 5; player must be within 100px with EMF Reader held; sources last 8–14s, max 6 queued
- [x] Visual fixes: furniture occlusion (room-gated draw), camera-on-furniture elevation (placedOn tag + 0.75× scale + Y offset), camera-on-tripod mount position (cy-8 offset)
- [x] CCTV camera station in van: monitor on north wall, E to enter feed, ESC to exit, ←/→ cycle cameras; drawCameraView() renders world from camera POV with 60° cone fog; no-signal screen if no cameras; _drawCCTVChrome() overlay (scanlines, REC dot, cycle hint)
- [x] Handheld camera viewfinder mode: holdingCamera replaces normal fog with _applyCameraConeFog (60° cone on main view); N key toggles NV (green tint + dark-green fog vs. grey normal); _drawViewfinderChrome corner brackets + NV/REC badges; PiP removed; _applyCameraConeFog extracted as shared helper used by both handheld and CCTV
- [x] Unified camera feed system: renderCameraFeed(tCtx,tFogCtx,W,H,zoom,camX,camY,facing) shared core; handheld PiP overlay (top-right, 234×176, PIP_ZOOM=1.2) when Video Camera selected; night vision always-on (NV tint + dark-green fog + no room-light block); handheld recording = R key (heldItemOn); CCTV recording = R key in view mode (cctvRecording); _drawPiPChrome() + updated _drawCCTVChrome()
- [x] Ghost roster system: GHOST_REGISTRY (27 ghosts), GHOST_CONFIG flags, GHOST_BEHAVIOR_DEFAULTS, ghostSpecialState, selectGhostForRound(), setupGhostSpecialState(), ghostLog(); normal roster live; Ultimate Ghost disabled
- [x] Ghost Orbs: camera-only floating orbs (4 per round); drift+bounce inside ghost's room; flicker alpha; visible only in handheld camera view and CCTV feed; ORB badge notification in both viewfinder and CCTV chrome when orb in cone
- [x] Ghost behavior from type: getGhostBehavior() wired to movement speed, event rate, roam tendency; specialRules enforced (dots_camera_only, no_room_migration); Goryo DOTS only visible through held camera viewfinder
- [x] Journal placement fix: Journal on van pegboard (not auto-equipped); tap G = instant drop closed; hold G (≥250ms) = placement preview; J during preview toggles OPEN/CLOSED state; active=open, inactive=closed; ghost writes in placed open journal; heavy ink scrawls + bleed tint for written state
- [x] Journal keybind page update: full control list (rebindable + fixed), scrollable right page with scroll indicator
- [x] Voodoo Doll input rework: E pushes random unused pin, R exits popup, no manual pin selection
- [x] Music Box functionality: R activates, ghost sings (pulse ring + ♪ note), direction arrow, loudness bar, 1.5s hunt trigger
- [x] Music Box break behavior: shatters into 2 inert floor pieces on hunt trigger, removed from hand
- [x] Tarot Cards functionality: R opens popup, E draws one card, 10-card shuffled deck, card art only, burn effect, 10 effects mapped
- [x] Pre-game lobby: warehouse staging area, van parked inside, corkboard start station, E/click to begin round
- [x] Ghost hunt mode: sanity drains over time; hunt triggers below threshold; ghost chases at huntSpeedLOS; catch = lostRound; Crucifix blocks hunt; Incense ends hunt early; blink/solid/stealth visibility per ghost type; red border + "! HUNTING !" HUD; music box/tarot/voodoo trigger real hunts; Ultimate Ghost removed
- [x] Phase 1 — Core sanity plumbing: setSanity() helper, route all playerSanity mutations through helpers, finalSanity in results popup, gameRunning guard on passive drain
- [x] Phase 2 — Hunt + sanity events: Demon 100% threshold override, _endHunt() centralized helper, huntCooldownMult (Demon 0.55×), event drain hooks (spirit box 3%, EMF5 2%, ghost writing 3%, ouija 5%+10%), light flicker during hunts
- [x] Phase 3 — Ghost AI: playerNoise + makeNoise() + ghostHearNoise(), sprint/door noise generation, stuck detection with 2.5s timer + forced reset, ghost hears sprint footsteps + door slams
- [x] Phase 4 — Evidence system audit: EMF held-reader 120px / placed-reader 150px (player must be within 160px of placed reader), orb camera-only already enforced, freeze temp confirmed working
- [x] Phase 5 — Player systems: Ouija break 20% cursed hunt trigger (Demon immune), sprint in dark room +0.8%/s extra sanity drain, held EMF4+ noise 0.4/s, held Spirit Box noise 0.35/s
- [x] Phase 6 — Map/environment: breaker starts OFF, random light states (~40% on), breaker flip emits makeNoise(0.6), prompt shows ON/OFF with color
- [x] Phase 7 — Sanity bar HUD (top-left, color-coded), Shade no-hunt within 60px, Onryo flame_blocks_hunt (threshold→0 near active flames), Onryo hunt-on-extinguish (65% chance)
- [x] Phase 8 — Deogen hunt slow-near-player (fast far, crawl close), Hantu cold-speed (2.0× cold / 0.8× warm), Jinn can't approach breaker
- [x] Phase 9 — Poltergeist mass-throw (2–4 EMF spikes + sanity drain), Banshee wail on roam (−4% sanity within 250px)
- [x] Phase 10 — Firelight deployable candle: pegboard, lighter-light mechanic, ghost-extinguish (30% per pass), candlelight glow, contextual E prompt
- [x] Phase 11 — Sanity Meds usable (R key consumes, +35% sanity), "R to use" HUD hint
- [x] Phase 12 — Yokai voice aggression: Spirit Box response cuts hunt cooldown −8s
- [x] Phase 13 — Light flicker: pre-event subtle flicker (ghostEventTimer < 1.5s), power-off drain multiplier ×1.6
- [x] Phase 14 — Debug HUD: playerNoise %, power state, ghostStuckTimer added to readout
- [x] Phase 15 — Raiju disrupts electronics (turns off active items within 100px during hunt, occasional power cut)
- [x] Phase 16 — The Twins dual presence: decoy EMF in second room every 8–22s
- [x] Phase 17 — Mimic form switching: changes copied ghost every 60–120s, getGhostBehavior routes through mimicTarget
- [x] Phase 18 — Moroi: speed_scales_with_low_sanity (1.0× at 100% → 2.0× at 0%), spirit_box_curse (−5% on response)
- [x] Phase 19 — Hunt warning UI: amber border + "⚠ GHOST NEARBY ⚠" text; hunt shows remaining time
- [x] Phase 20 — Ghost flips breaker: non-Jinn ghosts passing within 55px of breaker have 6% chance to cut power
- [x] Phase 21 — Firelight candle fully wired to Onryo extinguish detection and getHuntThreshold block
- [x] Phase 22 — Van minimap: EMF hotspot blips from placed active readers; ghost blip when EMF5 fires
- [x] Phase 23 — Round results: XP level-up check (30% scaling), level displayed with LEVEL UP! badge, XP progress bar
- [x] Phase 24 — Hiding mechanic: still + small closet room builds hideTimer (max 8s); catch radius 18→10px; ghost gives up hunt 5→3s when hiding; noise decays 3× faster; HUD indicator
- [x] Phase 25 — Balance pass: Demon frequent_hunts (1.5–2.5s check), power-off mult 1.6→1.35, Shade radius 60→80px, Revenant LOS speed switch, Mare light-off weighting, Yurei door-closing behavior
- [x] Phase 26 — Salt item: pegQty 0→2, world visual (pristine/stepped states), ghost step detection (Wraith skips), UV footprint marks (boot-print style, distinct from fingerprints)
- [x] Phase 27 — Audio system: lazy AudioContext, _playTone/_playNoise helpers, 6 named sounds (EMF tick, hunt warning, hunt pulse, ghost event crackle, door slam, salt crunch); _tickAudio(delta) drives timing
- [x] Phase 28 — Spirit Box static (noise bursts every 100–160ms), voice response sweep on confirmed response; D.O.T.S. dual-harmonic ping on disturbance start
- [x] Phase 29 — Photo Camera evidence: pegQty 0→1, R key captures nearby evidence types (7 types), flash overlay, shutter sound, bonus XP in results (+25 ghost, +10/type)
- [x] Phase 30 — Ambient soundscape: house creaks (dark vs lit rooms), water drips (Bath/Laundry/Kitchen), low-sanity heartbeat scaling with panic; all gated on safeTimeOver && !ghostHunting
- [x] Phase 31 — Footstep sounds: player surface-based footsteps (wood/tile/concrete/grass/van), ghost footsteps during hunt scaled by speed, Myling silenced
- [x] Phase 32 — Thermometer HUD badge (< 15°C, color-coded, ❄ when freezing) + frost vignette overlay in player's room when temp ≤ 0°C (animated sparkle dots)
- [x] Phase 33 — Ghost event visuals: EMF ripple pulses (orange/red expanding rings on L4-5), ghost interact marks (dark smear blobs at ghost position on interactNearby)
- [x] Phase 34 — Hunting improvements: Revenant "IT SEES YOU / SEARCHING" HUD cue during hunt; Yokai "HEARING YOU" pulsing warning when Spirit Box active within 200px
- [x] Phase 35 — Ghost behavior visuals: Onryo candle extinguish orange flash (radial burst), Poltergeist parabolic throw arc (brown rect flying through air per thrown item)
- [x] Phase 36 — Evidence system polish: journal auto-narrows when 3 confirmed; "NEW EVIDENCE" HUD flash; evidence count badge
- [x] Phase 37 — Incense system polish: smudge_extended_pacify (Spirit 180s via beh.incenseDuration), smudge_room_trap (Yurei locked in room during pacify), SMUDGED Xs HUD badge with pulsing alpha
- [x] Phase 38 — Proximity audio: sndGhostBreath() when ghost < 90px (non-hunt, 3.5–6.5s), sndWhisper() at sanity < 20% (8–20s), sndGhostMoan() during hunts (8–14s); 3 new timer state vars; reset in startRound()
- [x] Phase 39 — Ghost interaction audio + flash: sndDoorCreak() on touchDoor (ghost opens/closes), sndLightClick() on touchLightSwitch; both bump ghostAppearTimer for brief 0.6–1.2s flash appearance
- [x] Phase 40 — Evidence detection flashes + hunt pressure scaling: EMF L5 red flash (2s), D.O.T.S. figure purple flash (2s), hunt check interval scales 1.5–2.5s at 0% sanity → 4–6s at 100% sanity
- [x] Phase 41 — Ghost specialRule pass: no_mist_form (Oni stays visible 8s on event), more_active_near_players (Oni event rate ×1.8 within 120px), high_parabolic_activity (Myling doubles EMF+interact marks per event)
- [x] Phase 42 — Crucifix charges system: 2 charges per Crucifix, blocks hunt START (not active hunt), consumes charge; spent Crucifix dims gold→dark; sndCrucifix() holy ring tone; "CRUCIFIX ACTIVATED" gold screen flash + edge glow
- [x] Phase 43 — Per-ghost hue tint: getGhostHue() maps 24 ghost names to CSS hue-rotate degrees; passive appearances (event/voodoo/tarot) use ghost-specific color; hunt states keep their red/orange tints
- [x] Phase 44 — Evidence detection flashes: Ghost Writing (purple), UV Fingerprint (violet), Freezing Temps (cyan); all 2s overlays with fade-in/out; freezing uses edge-detect on thermometerReading ≤ 0°C
- [x] Phase 45 — Ghost Orb notification + visual: "GHOST ORB DETECTED" flash on first camera sighting per round (using _orbsInCameraView); orb render enhanced with outer pulsing halo + larger glow radius (12→22px outer)
- [x] Phase 46 — Ghost scare events: random 60–180s interval; brief 0.4s ghost flash + 0.6s red screen + 5% sanity drain + sndGhostScream() (descending sawtooth shriek); only fires when ghost within 200px
- [x] Phase 47 — Ghost activity heat map on minimap: ghostActivityRooms tracks cumulative activity strength per room; drawn as red/orange overlay (opacity 0–32%) on van minimap, normalized to max room heat
- [x] Phase 48 — Ghost status HUD badges: "GHOST ATTRACTED" amber pulse when ghostAttractTimer > 0; Thaye age/aggression badge shows elapsed minutes + current hunt threshold (red → amber → green as ghost ages)
- [x] Phase 49 — Ghost presence detection: ghost enters player's room → sndColdWind() + "PRESENCE FELT" cyan overlay (3s); _prevGhostInRoom edge detect; gated on safeTimeOver && !ghostHunting
- [x] Phase 50 — Death sequence: 2.5s _deathTimer on catch; sndDeath() (static + shriek + thud); red flash → black-in → "ELIMINATED" text overlay; controls frozen during sequence
- [x] Phase 51 — Ghost LOS + speed rebalance: same-room check replaces 350px distance LOS; ghost chases stale _huntLastKnownX/Y at huntSpeedBase when player out of sight; gives up after 8s; huntSpeedBase 1.0→0.85, huntSpeedLOS 1.4→1.1; Deogen exempt (always_knows_player_pos); on-screen hunt/notification/evidence text overlays removed
- [x] Tilemap / multi-room map
- [x] Map 2 — Gallow's Inn: full room/door/doorway data, furniture, floor textures, flat motel roof, map-aware front door, map-aware fog light spill

## Done
- Project files created
- index.html: canvas, game loop, player movement, room boundary
- Title screen with Start button → transitions to game
- Room walls drawn as solid rects; player collides with inner floor boundary
- ESC returns to title screen; player position resets
- Notebook spawns at 1 of 3 fixed positions on game start
- Player can inspect notebook with E when nearby; shows found message
- Notebook reveals a randomly chosen ghost name on inspection
- Exit zone added; reaching it after notebook found ends the round with a result screen
- Ghost list replaced with name+clue objects; notebook shows clue, result screen reveals name
- Guess screen: loop fix, 1/2/3 keys and mouse click both work via shared selectGhost()
- Fixed duplicate loop bug: loopRunning flag prevents multiple loop() instances
- Polished all screen instructions: title, game HUD, guess screen, result screen
- Arrow key scroll prevention; objective text moved to bottom-right HUD
- Added photo evidence object (red); reveals clue2 per ghost; both clues shown in HUD
- Ghost clues redesigned with overlaps so both clues are required to identify correctly
- Journal screen (J key): shows found clues and filters possible ghosts by evidence
- ~~Round timer (90s lose condition)~~ — replaced with safe-time system
- Safe-time window added: 20s countdown, then "Danger" indicator; pauses during journal
- WASD fix: expanded movementKeys to include w/a/s/d alongside arrow keys
